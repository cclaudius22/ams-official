// TEMPORARY dev preview route for the chart primitives (Agent 2 / charting lane).
// Not a production surface — used to screenshot the shared layer for palette review.
// Safe to delete once the look is signed off.
import { ChartsPreview } from '@/components/charts/__preview__/ChartsPreview'

export default function ChartsPreviewPage() {
  return <ChartsPreview />
}
